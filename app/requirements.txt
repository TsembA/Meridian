# requirements.txt — Production dependencies for the Meridian FastAPI application.
# All versions pinned to exact releases for reproducible builds and Trivy scanning.
# Runtime: Python 3.11 (gcr.io/distroless/python3-debian12:nonroot)
# Update with: pip install --upgrade <package> && pip freeze | grep <package>

# ── Web framework ────────────────────────────────────────────────────────────
fastapi==0.115.5
uvicorn[standard]==0.32.1     # Includes uvloop and httptools for performance

# ── Database ─────────────────────────────────────────────────────────────────
sqlalchemy==2.0.36            # Async ORM
asyncpg==0.30.0               # Async PostgreSQL driver (no libpq required)

# ── Cache ─────────────────────────────────────────────────────────────────────
redis==5.2.0                  # redis-py with asyncio support (redis.asyncio)

# ── AWS SDK ──────────────────────────────────────────────────────────────────
boto3==1.35.36                # SSM Parameter Store reads at startup
botocore==1.35.36             # boto3 dependency — pin to matching version

# ── Observability ─────────────────────────────────────────────────────────────
prometheus-fastapi-instrumentator==7.0.0   # Auto-instruments all routes

# ── Configuration ─────────────────────────────────────────────────────────────
pydantic==2.9.2               # Data validation (v2 — field_validator, BaseModel)
pydantic-settings==2.6.1      # BaseSettings with env var support

# ── HTTP client (used in health check and tests) ──────────────────────────────
httpx==0.28.0                 # Required by TestClient in tests
