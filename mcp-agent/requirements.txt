# requirements.txt — Dependencies for the Meridian MCP diagnostic agent.
# All versions pinned for reproducible builds and Trivy vulnerability scanning.

# ── MCP framework ─────────────────────────────────────────────────────────────
mcp==1.0.0                    # Anthropic MCP Python SDK

# ── Kubernetes client ─────────────────────────────────────────────────────────
kubernetes==29.0.0            # Official k8s Python client — reads pod status and logs

# ── HTTP client (Prometheus, Alertmanager, GitHub API) ────────────────────────
httpx==0.26.0                 # Async HTTP with connection pooling

# ── AWS SDK ───────────────────────────────────────────────────────────────────
boto3==1.34.14
botocore==1.34.14

# ── Configuration and validation ──────────────────────────────────────────────
pydantic==2.5.3
pydantic-settings==2.1.0

# ── Logging ───────────────────────────────────────────────────────────────────
# (stdlib only — no extra packages needed)

# ── Testing (dev only — not installed in production image) ───────────────────
# pytest==7.4.4
# pytest-asyncio==0.23.3
# pytest-mock==3.12.0
