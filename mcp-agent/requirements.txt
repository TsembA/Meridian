# requirements.txt — Dependencies for the Meridian MCP diagnostic agent.
# All versions pinned for reproducible builds and Trivy vulnerability scanning.
# Runtime: Python 3.11 (gcr.io/distroless/python3-debian12:nonroot)

# ── MCP framework ─────────────────────────────────────────────────────────────
mcp==1.23.0                   # Anthropic MCP Python SDK (CVE-2025-53365/53366/66416 fixed in 1.23.0)

# ── HTTP server (FastMCP SSE transport needs uvicorn) ─────────────────────────
uvicorn==0.32.0               # ASGI server — FastMCP calls it internally for SSE/HTTP transport

# ── Kubernetes client ─────────────────────────────────────────────────────────
kubernetes==31.0.0            # Official k8s Python client — tracks k8s 1.31 API

# ── HTTP client (Prometheus, Alertmanager, GitHub API) ────────────────────────
httpx==0.28.0                 # Async HTTP with connection pooling

# ── Configuration and validation ──────────────────────────────────────────────
pydantic>=2.11.0
pydantic-settings==2.6.1

# ── Logging ───────────────────────────────────────────────────────────────────
# (stdlib only — no extra packages needed)

# ── Testing (dev only — not installed in production image) ───────────────────
# pytest==8.3.3
# pytest-asyncio==0.24.0
# pytest-mock==3.14.0
